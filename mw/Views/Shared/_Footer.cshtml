<hr />
<footer class="text-center">
    <p>@LogInOut()</p>
</footer>

@helper LogInOut()
{
if (User.Identity.IsAuthenticated) {
        <text>@Html.ActionLink("Log off", "LogOff", "Account")</text>
} else {
        <text>@Html.ActionLink("∞", "LogIn", "Account")</text>
}
}