@if (User.Identity.IsAuthenticated && User.IsInRole("Admin")) {
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @*@Html.ActionLink("Admin", "Index", "Entries", new { area = "" }, new { @class = "navbar-brand" })*@
                @Html.RouteLink("Admin", "Home", null, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Entries <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("New", "Create", "Entries")</li>
                            <li role="separator" class="divider"></li>
                            <li>@Html.ActionLink("Manage", "Manage", "Entries")</li>
                        </ul>
                    </li>

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Projects <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("New", "Create", "Projects")</li>
                            <li role="separator" class="divider"></li>
                            <li>@Html.ActionLink("Manage", "Manage", "Projects")</li>
                        </ul>
                    </li>

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Settings <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("New", "Create", "Entries")</li>
                            <li role="separator" class="divider"></li>
                            <li>@Html.ActionLink("Manage", "Manage", "Entries")</li>
                        </ul>
                    </li>
                </ul>

                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
}